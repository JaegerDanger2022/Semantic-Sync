# Semantic Sync Monorepo

This repo contains:

- `backend/`: FastAPI server that proxies Elasticsearch + Kibana Agent APIs.
- `extension/`: VS Code extension for indexing and chat.

See `plan.md` for the full blueprint.
